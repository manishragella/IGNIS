// Firebase config — NOT USED IN DEMO MODE
// Placeholder to prevent import errors if any old imports remain
export const auth = null;
export const db = null;
export const storage = null;
export default null;
